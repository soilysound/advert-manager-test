function imgsrc(path, size, breakpoint) {
  const src = path.replace(/{width}x{height}/, size);

  if (typeof breakpoint === 'string') {
    return `${src} ${breakpoint}`;
  }

  return src;
}

module.exports = imgsrc;
