const imgsrc = require('./imgsrc');
const jsonData = require('./json-data');
const renderFile = require('./render-file');
const renderIcon = require('./render-icon');
const translate = require('./translate');
const limit = require('./limit');
const interpolate = require('./interpolate');

const helpers = {
  imgsrc,
  jsonData,
  renderFile,
  renderIcon,
  translate,
  limit,
  interpolate
};

module.exports = helpers;
